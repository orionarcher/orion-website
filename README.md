# Orion Archer Cohen

Personal website built with plain HTML and CSS.

Edit `index.html` for content and `styles.css` for presentation. Preview locally with `python3 -m http.server 4173`.

Netlify deploys the repository root. No build command or dependencies are required. Push changes to the production branch to publish through the connected Netlify project.
